## Shared ESP-IDF components (esp32-s3-m5)

This directory holds **reusable ESP-IDF components** shared across the `esp32-s3-m5/*` tutorial projects.

Currently:

- `animatedgif/`: **bitbank2/AnimatedGIF** ported for ESP-IDF (used by `0013` and `0014`).


