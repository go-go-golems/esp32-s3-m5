# Tasks

## TODO

- [x] `T01` Define settings screen requirements and identify where to attach it in LVGL list flow
- [x] `T02` Add `Settings` action entry and dedicated settings view state in `app_main.cpp`
- [x] `T03` Render current Wi-Fi IP (or disconnected state) in settings view via `wifi_mgr_get_status`
- [x] `T04` Add back-navigation from settings to timer view (encoder click on `Back`)
- [x] `T05` Remove horizontal scrollbar artifact by constraining scroll direction and label clipping
- [x] `T06` Build + flash firmware and validate on hardware
- [x] `T07` Verify by screenshot that settings view shows IP and timer view no longer has bottom horizontal scrollbar
- [x] `T08` Update ticket docs/changelog/diary with implementation evidence
