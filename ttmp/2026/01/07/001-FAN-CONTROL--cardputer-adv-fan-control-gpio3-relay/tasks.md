# Tasks

## TODO

- [ ] Verify GPIO3 is wired to relay input (and document relay polarity)
- [ ] Build/flash `0037-cardputer-adv-fan-control-console` and confirm `fan on|off` works
- [ ] Validate animation verbs: `fan blink`, `fan strobe`, `fan tick`, `fan beat`, `fan burst`
- [ ] Add a simple non-TTY command sender script (optional) for automated validation
