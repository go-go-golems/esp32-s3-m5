# Tasks

## TODO

- [ ] REPL history + arrow key navigation (LineEditor)
- [ ] JS multiline/continuation prompt (LineEditor + JsEvaluator)
- [ ] Improve exception output (include stack/trace if available)
- [ ] Storage commands: `:ls`, `:cat`, `:rm` (SPIFFS)
- [ ] Deterministic autoload ordering (sort `.js` files before eval)
- [ ] Make `load(path)` streaming / raise size limit safely
- [ ] Keyboard-driven REPL input (Cardputer: no PC required)
- [ ] Display output mirroring / tee console
- [ ] Optional speaker feedback hooks
- [ ] Memory budget validation on Cardputer (measure peak heap/JS usage; document)
- [ ] Make device scripts default to `/dev/serial/by-id` everywhere
