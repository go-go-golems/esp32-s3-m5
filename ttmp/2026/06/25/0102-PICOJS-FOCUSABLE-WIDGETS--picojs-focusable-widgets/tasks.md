# Tasks

## Done

- [x] Add focusable widget selection movement (`move`, clamped `select`, `value`).
- [x] Implement real `onPick(fn)` storage and callback execution.
- [x] Add deferred `OS.launch(name)` request plumbing from PicoJS runtime to PicoOS supervisor.
- [x] Validate menu navigation and Enter-to-launch on ESP32-P4 hardware.

## TODO

- [ ] Add disabled-item semantics and scroll-window rendering for long menus.
