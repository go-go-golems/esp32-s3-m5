// Placeholder built asset.
// Run `npm ci && npm run build` in `web/` to generate the real Preact+Zustand bundle.
document.getElementById('app')?.appendChild(document.createTextNode('Build web assets to enable the UI.'));

